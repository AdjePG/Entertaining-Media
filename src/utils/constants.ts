export const LDSCHEME_STATUS = Object.freeze({
  SYSTEM: "light dark",
  LIGHT: "light",
  DARK: "dark"
})

export const CONTENT_TYPE = Object.freeze({
  MOVIES: 0,
  GAMES: 1,
  BOOKS: 2
})

export const VIEW_TYPE = Object.freeze({
  ROWS: "selection",
  GRID: "all"
})

export const DIRECTION_ROW_MOVE = Object.freeze({
  LEFT: 0,
  RIGHT: 1
})